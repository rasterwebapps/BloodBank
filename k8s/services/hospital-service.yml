# hospital-service Service — ClusterIP (internal only)
# Namespace options: bloodbank-dev | bloodbank-staging | bloodbank-uat | bloodbank-prod
apiVersion: v1
kind: Service
metadata:
  name: hospital-service
  namespace: bloodbank-prod
  labels:
    app.kubernetes.io/name: hospital-service
    app.kubernetes.io/part-of: bloodbank
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hospital-service
  ports:
    - name: http
      port: 8086
      targetPort: 8086
      protocol: TCP
