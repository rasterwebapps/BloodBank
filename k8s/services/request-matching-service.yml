# request-matching-service Service — ClusterIP (internal only)
# Canary: selector omits track so both stable and canary pods receive traffic
# Namespace options: bloodbank-dev | bloodbank-staging | bloodbank-uat | bloodbank-prod
apiVersion: v1
kind: Service
metadata:
  name: request-matching-service
  namespace: bloodbank-prod
  labels:
    app.kubernetes.io/name: request-matching-service
    app.kubernetes.io/part-of: bloodbank
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: request-matching-service
  ports:
    - name: http
      port: 8087
      targetPort: 8087
      protocol: TCP
