# transfusion-service Service — ClusterIP (internal only)
# Blue-Green: switch selector version: blue → green to cut over
# Namespace options: bloodbank-dev | bloodbank-staging | bloodbank-uat | bloodbank-prod
apiVersion: v1
kind: Service
metadata:
  name: transfusion-service
  namespace: bloodbank-prod
  labels:
    app.kubernetes.io/name: transfusion-service
    app.kubernetes.io/part-of: bloodbank
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: transfusion-service
    version: blue
  ports:
    - name: http
      port: 8085
      targetPort: 8085
      protocol: TCP
