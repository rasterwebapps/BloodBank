# document-service Deployment (Rolling strategy)
# Namespace options: bloodbank-dev | bloodbank-staging | bloodbank-uat | bloodbank-prod
apiVersion: apps/v1
kind: Deployment
metadata:
  name: document-service
  namespace: bloodbank-prod
  labels:
    app.kubernetes.io/name: document-service
    app.kubernetes.io/part-of: bloodbank
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: document-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: document-service
        app.kubernetes.io/part-of: bloodbank
    spec:
      initContainers:
        - name: wait-for-config-server
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until wget -q --spider http://config-server:8888/actuator/health; do
                echo "Waiting for config-server..."
                sleep 5
              done
      containers:
        - name: document-service
          image: bloodbank/document-service:${IMAGE_TAG}
          ports:
            - containerPort: 8091
          envFrom:
            - configMapRef:
                name: bloodbank-shared-config
            - configMapRef:
                name: bloodbank-services-config
          env:
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: bloodbank-secrets
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: bloodbank-secrets
                  key: DB_PASSWORD
            - name: RABBITMQ_USERNAME
              valueFrom:
                secretKeyRef:
                  name: bloodbank-secrets
                  key: RABBITMQ_USERNAME
            - name: RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: bloodbank-secrets
                  key: RABBITMQ_PASSWORD
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: bloodbank-secrets
                  key: MINIO_ACCESS_KEY
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: bloodbank-secrets
                  key: MINIO_SECRET_KEY
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8091
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8091
            initialDelaySeconds: 60
            periodSeconds: 20
            failureThreshold: 3
