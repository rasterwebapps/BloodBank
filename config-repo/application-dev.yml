# Development profile overrides
spring:
  jpa:
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.bloodbank: DEBUG
    org.springframework.security: DEBUG
